apiVersion: v1
kind: Service
metadata:
  name: prometheus-svc
  namespace: monitoring
spec:
  selector: 
    app: prometheus-server
  type: NodePort
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090 
